# 风笺回廊：引擎与控制器验收记录

执行时间：2026-09-07 21:13:28–21:13:29（Asia/Shanghai；UTC 13:13:28–13:13:29）。

运行环境：Windows_NT 10.0.26340，x64，Intel Core i7-10700 @ 2.90 GHz，Node.js v24.18.0。以下性能数据来自本机 Node 进程，不代表微信真机帧率或广告服务可用性。

## 自动化测试

命令：`node --test tests/*.test.js`

结果：51 项通过，0 项失败，约 258 ms。涵盖引擎、广告回调、平台适配、本地存储与控制器集成。

新增的 `tests/integration.test.js` 共 8 项，使用实际 Game、Renderer、广告模块及存储模块，通过内存平台和 Canvas 替身检查：

- 首关完成记录三星、解锁第二关、清除进行中路线。
- 退出后按已保存动作重放，位置、回声、收集物与灯火一致。
- 广告中途关闭不续灯；完整结束后续灯一次；重启仍保留已使用的续灯机会。
- 微信环境未配置广告位时不发放预览奖励。
- 昨日每日挑战跨日恢复、重玩后，成绩仍记入原日期；每日星级不混入主线星级。
- 无法重放的存档清理后保留已有通关记录。
- 320×568、390×844、768×1024 三组窗口及安全区域下，30 关和每日地图均不向 Canvas 传入 NaN/Infinity；回声预告与方向键不重叠。
- 第 0–3 拍回声预告准确保留尚未存在的空位。

## 1,000 个每日日期

日期范围：2026-01-01 至 2028-09-26，包含 2028 年闰日。

| 指标 | 结果 |
| --- | ---: |
| 生成并成功按见证路线通关 | 1,000 / 1,000 |
| 不同地图及对象配置 | 1,000 |
| 含有风格的地图 | 616 |
| 验证过的有效见证动作 | 18,752 |
| 最短 / 最长解 | 10 / 32 拍 |
| 平均解长 | 18.752 拍 |
| 生成时间中位数 | 0.521 ms |
| 生成时间 P95 | 1.103 ms |
| 平均生成时间 | 0.569 ms |
| 最大生成时间 | 2.931 ms（2026-01-09） |

每张地图均满足：见证动作没有撞墙；未复活即可通关；全部信笺及邮票收齐；最后位于出口；实际拍数等于 `par` 并获得三星。前 25 张地图另外交给独立的 `tools/solve.js` 参考 BFS 求解，得到的最短步数全部与每日生成器的 `par` 相等。

这批数据验证了所列日期，没有将有限样本结果推断成所有未来日期或所有设备的性能保证。

## 50,000 次随机动作检查

使用固定随机种子 `20260907`，地图池包含 30 个主线关卡及 50 张每日地图，共 80 张。输入包含四个方向、等待以及非法字符串和空值；失败后随机尝试复活或换图重新开始。

| 指标 | 结果 |
| --- | ---: |
| 输入尝试 | 50,000 |
| 推进回合的有效输入 | 22,224 |
| 被墙、边界或输入校验拒绝的输入 | 27,776 |
| 复活次数 | 363 |
| 灯火耗尽次数 | 1,007 |
| 随机路线通关次数 | 8 |
| 重新开始次数 | 652 |
| 不变量失败 | 0 |

逐次检查了：原状态不被修改；玩家不出界且不位于墙内；历史长度等于回合数加一；历史起点及末点正确；回声严格延迟三步；每回合灯火扣除与一次性补给一致；收集物始终属于原地图且不重复；信笺只能由玩家当前位置领取，邮票只能由回声当前位置领取；等待不改变玩家位置；拒绝的动作不改变状态；结束状态不再接受输入；胜利必须满足出口及全收集条件。

## 集成审查中已修复的问题

1. 每日挑战跨午夜恢复后重玩曾错误使用新日期计分，现由每日地图 ID 绑定原日期，已有回归测试。
2. 前两拍回声预告曾把将来位置提前放到第一格，现补足左侧空位，已有回归测试。
3. 小屏下回声预告曾与向上按钮重叠，现缩小棋盘可用高度，已有回归测试。
4. 平台取消触摸曾映射为释放动作，平台模块现将其作为取消，另有平台层回归测试。

真实微信激励广告回调、真机显示、声音与触摸手感仍需在配置有效广告位并具备微信开发者工具/真机运行条件后验收；本记录不声称已执行真实广告播放。
