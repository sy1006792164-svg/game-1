# 连续选关更新（1.7.0）

更新日期：2026-09-08。999关地图和难度保持内容版本5，已有主线存档正常续玩。

选关取消上一章、下一章和分页计数。章节标题作为连续列表中的分组，保留双列关卡卡片；顶部显示整体进度，并提供“回到进度”。进入选关时自动定位当前待完成关卡所在章节，末章按列表底部边界定位。

触摸、鼠标拖动及滚轮均可连续滚动，松手有惯性，边缘带阻力和回弹；卡片渐显、按压缩放，当前待完成关卡用暖色边框强调。选关与邮票册共用`src/list-scroll.js`的手势逻辑，但分别保存各自的滚动状态。

按视口计算需要绘制的章节与卡片，避免每帧绘制999张卡片。动画记录最多保留64条；裁切区域外的卡片不能触发点击。拖动、停止惯性和导航切换不会误入关卡。

验证：

- `npm.cmd test`：122项通过，0失败、0跳过；包括主线999关通关回放和既有邮票册回归。
- 遍历全部章节，确认001–999均能出现、无第1000关；测试尺寸下每帧最多14张卡片命中区域，动画记录不超过64条。
- 验证跨章连续显示、进入时定位、末关定位、返回进度、触摸拖动不误进关卡、标题区域裁切，以及键盘滚动与棋盘镜头相互独立。
- 浏览器390×844和320×568检查章节衔接、惯性拖动、列表末尾、回到第003关和轻触开局，无运行错误。
- 构建、源码/资源检查通过。新增选关交互尚未进行微信真机复验。
